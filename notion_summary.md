# 🎟️ [종합 정리] 대규모 트래픽 선착순 쿠폰 발급 시스템

> **팀명**: 2조 투게더 (박종범, 이용재, 이헌진, 정문구, 박찬영)  
> **과정**: LG유플러스 유레카 백엔드 개발자 종합프로젝트  
> **프로젝트 목표**: 10,000장 한정 쿠폰에 동시 20,000건 폭주 시 **초과 발급 0건(NFR-1)** 보장 및 **300만 건 데이터 무결성 검증**

---

## 1. 📌 프로젝트 개요 및 3대 핵심 과제

1. **동시성 제어 (Race Condition)**: 10,000개 재고에 20,000명이 동시 접근할 때 오버셀 0건 달성
2. **1인 1매 엄격 제한**: 광클, F5 새로고침, 멀티 탭, 매크로 봇을 통한 중복 발급 원천 차단
3. **RDB 커넥션 병목 격리**: 폭주 트래픽이 HikariCP(커넥션 풀)를 고갈시켜 서버가 마비되는 연쇄 장애 차단

---

## 2. 🔍 현업 시스템 조사와 의문: "왜 Redis + Kafka인가?"

1. **현업 서비스 조사 (배민, 무신사, 인터파크 등)**:
   - 대규모 트래픽 기업들의 공통 아키텍처: **"Redis (인메모리) + Kafka (메시지 큐)"** 표준 패턴 확인
2. **팀의 엔지니어링 의문 (Why?)**:
   - *"남들이 쓴다고 무작정 도입하는 것은 엔지니어링이 아니다."*
   - *"왜 순수 Java 락이나 DB 락으로는 안 될까? 단순 Redis 명령어로는 왜 동시성이 터질까?"*
   - *"Kafka만 도입하면 정말 무조건 빨라질까? 실제 병목은 어디에 있는가?"*
3. **가설과 실측 기반의 8단계 검증 여정**:
   - 가장 원초적인 **순수 Java(락 없음)부터 DB 비관적 락, Redis Lua, Kafka Consumer 최적화**까지 직접 부하를 걸어 한계를 깨뜨려보고, 실측 수치로 최적의 아키텍처를 도출하기로 결정.

---

## 3. 🏗️ 아키텍처 철학 & 3계층 역할 분담

> **"임시 상태와 빠른 판단은 Redis, 트래픽 완충은 Kafka, 영구 기록과 무결성은 MySQL"**

```
[클라이언트 20,000명] 
       │ 
       ▼
[1계층: 대기열 & Redis] ──▶ 0.001초 만에 선착순 1만 명 컷 & 1인 1매 원자적 검증 (초과 트래픽 0.5ms 즉시 차단)
       │ (성공 10,000건만 통과 / 즉시 202 응답)
       ▼
[2계층: Kafka Buffer]  ──▶ 발급 성공 이벤트를 디스크 큐에 비동기 완충 (DB 쓰기 속도와 분리)
       │
       ▼
[3계층: Kafka Consumer]──▶ 100건 청크 단위 JDBC Batch Insert로 일괄 적재 (DB 부하 최소화)
       │
       ▼
[4계층: MySQL (SSOT)]  ──▶ 복합 유니크 제약(policy_id, user_id)으로 최종 멱등 영구 저장
```

---

## 3. 🧪 6단계 점진적 아키텍처 진화 (실측 데이터)

| 단계 | 적용 방식 | 처리 속도 / TPS | 정합성 결과 | 실패 원인 및 핵심 평가 |
|---|---|---|---|---|
| **1단계** | 순수 Java (락 없음) | 41ms | ❌ **54건 초과 발급** | 동시성 경합으로 Check-then-Act 원자성 파괴 |
| **2단계** | AtomicInteger | 42ms | ⚠️ 0건 (카운트 정확) | JVM 메모리 종속, 서버 재시작/다중화 환경 불가 |
| **3단계** | RDB 비관적 락 | 타임아웃 발생 | ⚠️ 0건 (정합성 성공) | 2만 건 동시 인입 시 HikariCP 커넥션 풀 완전 고갈 |
| **4단계** | Redis 단순 DECR | 3,612ms | ❌ **6,646건 중복 발급** | 조회와 차감 사이 틈새로 동시성 침투 |
| **5단계** | Redis Lua + 동기 DB | 12,706ms | ✅ 0건 | DB 커넥션 병목으로 응답 지연 심각 (최대 7.4초 대기) |
| **6단계** | Redis Lua + Kafka(단건) | 10,858ms | ⚠️ **283건 오류 발생** | 컨슈머 단건 저장으로 오히려 순수 Redis보다 악화 |
| **최종** | **Redis Lua + Kafka Batch + 2-Set** | **6.0초 (3,358 req/s)<br>Max 43.88ms** | **✅ 0건 (완벽)** | **초과발급 0건, P50 46ms, 무결성 100% 달성** |

### 💡 핵심 기술적 전환점 3가지
1. **Redis Lua Script (EVAL)**: 재고 확인(`SCARD`) + 중복 확인(`SISMEMBER`) + 차감/등록(`SADD`)을 단일 원자 연산으로 실행하여 **별도 분산락(Redisson) 대기 시간 없이 오버셀 0건** 달성.
2. **SEQUENCE 채번 & JDBC Batch Insert**: MySQL `IDENTITY`의 즉시 flush 강제 문제를 `SEQUENCE` 메모리 사전할당(50개)으로 해결하고, `rewriteBatchedStatements=true`로 **DB 처리량 5.5배 개선 (229 TPS ➜ 1,250 TPS)**.
3. **2-Set (RESERVED ➜ ISSUED) 비동기 즉시 응답**: 1단계 임시 예약 후 1ms 만에 202 응답, DB 저장 완료 시 확정 승격하여 **최대 지연 시간 6.24초 ➜ 43.88ms (140배 단축)**.

---

## 4. 🛡️ 실전 트러블슈팅 & 5계층 방어 로직

### ① 트래픽 폭주와 네트워크 유실 극복 (대기열 도입)
- **문제**: 2만 VU 부하 시 로컬 OS의 TCP 소켓 포트(65,535개) 고갈(TIME_WAIT)로 **29.77% 연결 거부 발생**.
- **해결**: 입구 맨 앞단에 **동적 스케일링 대기열 & SSE 실시간 통보 시스템**을 구축.
  - **대기열 등록 & 멱등성**: Redis ZSET에 `{userId: timestamp}` 등록 (F5 연타 시에도 ZSCORE로 기존 순번 멱등 유지).
  - **분산 락 스케줄러 & 동적 스케일링**: 1초 주기 `QueueAdmissionScheduler`가 분산 락(SETNX) 획득 후 대기열 부하에 따라 `QueueLimitAdminService`로 입장 정원 자동 스케일링.
  - **ZPOPMIN 원자적 추출 & 파이프라이닝**: `Math.min(batchSize, stock)` 슬라이싱 후 `ZPOPMIN`으로 선두 추출 ➜ Redis Pipelining으로 60초 TTL `activeToken` 일괄 등록.
  - **SSE(Server-Sent Events) 실시간 푸시**: 클라이언트 폴링 부하 없이 SSE로 토큰 실시간 전달 ➜ `/issue` 호출 시 토큰 소유자(userId) 일치 여부를 원자적으로 소비/검증하여 매크로/우회 100% 차단.

### ② 선착순 공정성 수호 (5대 역전 시나리오 방어)
- 대기열 줄서기: Redis 단조 증가 시퀀스로 **역전 0% 보장**.
- 마지막 10장 경계선: `Math.min(batch, stock)` 정밀 슬라이싱으로 **11등의 새치기 100% 차단**.
- 잠수 유저: 60초 TTL 만료 시 뒷사람에게 자동 양도 (정상 추월).

### ③ Redis 완전 유실(FLUSHALL) 카오스 복구
- **문제**: 부하 도중 Redis가 날아갔을 때 섣불리 복구하면 중복 발급 사고 발생.
- **해결 (`RedisRecoveryService`)**:
  - **Kafka Lag 안전 방어선**: 컨슈머 랙(Lag > 0)이 남아있으면 복구 진입 차단 (409 Conflict).
  - **SSOT 원자 복원**: DB 저장이 완료(Lag=0)되는 순간 MySQL 실제 발급량을 기준으로 **잔여 재고 116ms 만에 100% 오차 없이 복원** (10,000장 완벽 보존).

---

## 5. 🔍 300만 건 대규모 정합성 검증 체계

### ① XOR 체크섬의 한계와 Set 기반 Diff 채택
- **체크섬 검토 및 탈락 사유**:
  - 초기에는 빠른 집계를 위해 `BIT_XOR(user_id)`를 검토했으나,
  - 서로 다른 두 이상 건이 우연히 같은 값을 내는 **오류 상쇄(Collision)** 위험과,
  - 동일 유저 중복 발급 시 $A \oplus A = 0$으로 소멸되어 **중복 발급을 감지하지 못하는 치명적 약점**을 확인하고 **체크섬 방식 전격 탈락**.
- **최종 구현 (Set 기반 대칭차집합 & 불변식 엔진)**:
  - MySQL 커버링 인덱스를 활용해 DB 발급자 `Set<Long>` 추출 + Redis 확정 발급자 `Set<Long>` 로드
  - 메모리 내에서 $O(N)$ **대칭차집합(`redisOnly`, `dbOnly`) 연산**으로 불일치 유저를 정밀 색출

### ② 5대 정합성 검증 엔진 & 실측 성과
- **5대 검증 엔진**:
  1. **오버셀 검증**: `DB 발급수 > 총 재고 수량` 위반 검사 (NFR-1 절대 수호)
  2. **Redis ↔ DB Diff 검증**: Redis 확정 발급 유저 집합과 DB 발급 유저 집합 대칭차집합 대조
  3. **재고 누수 검증**: `Redis 잔여재고 + DB 발급수 + RESERVED 잔량 = 총 수량` 삼각 불변식 검증
  4. **생애주기 정합성 검증**: 발급(ISSUED) ➜ 사용(USED) ➜ 취소 간 `CouponIssue`와 `CouponHistory` 상태 동기화
  5. **FCFS 선착순 검증**: 대기열 도착 상위 N명 집합(`queue_join_log`)과 실제 DB 발급자 집합 일치 검증
- **실측 벤치마크 (정책 3개, 각 100만 건 스캔)**:
  - `clean-1m`: **0건 불일치 (6.3초 만에 SUCCESS)**
  - `mismatch-overissue`: 사전 주입된 초과발급 1,300건을 **6.2초 만에 정확히 적발**
  - `lifecycle-anomaly`: 사전 주입된 이력 누락 1,000건을 **6.1초 만에 정확히 적발**
  - **자원 안정성**: 300만 건 전수 검증 중 Spring Boot 힙 메모리 **1.45GB 안정 유지 (OOM 0%)**
  - **결과물**: 불일치 발생 시 원인 추적용 **CSV 리포트 자동 생성 및 다운로드 제공**.

---

## 6. 🏆 최종 핵심 성과 요약

- 🎯 **동시성 정합성**: 동시 20,000건 인입 시 **초과 발급 0건 (무결성 100%)**
- ⚡ **초저지연 응답**: 2-Set 구조로 **최대 지연 6.24초 ➜ 43.88ms (140배 단축)**
- 🚀 **DB 쓰기 최적화**: SEQUENCE + JDBC Batch로 **Consumer 처리량 5.5배 개선 (1,250 TPS)**
- 🛡️ **장애 자가 치유**: Kafka Lag 방어선 + SSOT 원자 복구로 **Redis 전멸 시 100% 무결점 복원**
- 🔍 **대용량 신뢰성**: **300만 건 정합성 검증 배치 6.2초 완료 및 CSV 리포팅 자동화**
